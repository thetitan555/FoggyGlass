# Tickets — P1.1 Reconciliation: Character-A movement + geometry Y-fix

> Owned by the **Architect**. Decomposed from the reconciliation work-order
> (`/docs/briefs/character-a-movement-reconciliation.md`), the intent brief
> (`/docs/briefs/character-a.md`), and the rulings this session recorded:
> **AD-037** (vertical convention / box Y-fix), **AD-038** (held-input looping-state
> exit), **AD-039** (airborne-action model), and the new `/docs/spec/trace-harness.md`.
> Each ticket names the spec/AD it serves, its dependencies, and the acceptance
> criteria it must satisfy (QA verifies against the spec's criteria, not the ticket's
> prose). **P1.1 is not done on headless green** — a human re-gate drives the brief
> checklist afterward (`pipeline-analysis-completeness-gap.md`).

## Sequencing (per-ticket dispatch — the P1.1/P2 default)

Per-ticket dispatch (protocol § "Token economy"): **one ticket per Developer subagent,
in dependency order, each ending on a real checkpoint.** These four share the movement
spec as a read but each lands a distinct, separately-verifiable checkpoint and one
(TKT-…-03) carries an engine change, so **no cluster is warranted** — the spec-read
overlap does not outweigh the blast-radius separation.

```
01 trace-harness ─▶ 02 geometry-Y-fix ─▶ 03 held-input-stances ─▶ 04 airborne-actions
   (the instrument     (AD-037 box data      (AD-038 walk exit +      (AD-039 dir/diag
    the rest verify      reflection;           crouch stance/block)     jumps + air
    through)             render is correct)                             normals)
```

Dispatch order and why:

1. **TKT-P1.1R-01 (trace harness)** first — it is the **verification instrument** the
   work-order folds in. Building it first lets the Developer verify each movement fix
   below **through it** as they land (the brief checklist becomes assertions). It is
   blind to render bugs (documented), so it shrinks but does not remove the human gate.
   *Checkpoint:* a string compiles to a buffer, replays headless through
   `RecordPlaybackSource`, and a smoke assertion (e.g. "idle stays idle") passes.
2. **TKT-P1.1R-02 (geometry Y-fix)** next — **AD-037** is the single root cause of the
   whole gate-2 box-appearance cluster, and fixing it unblocks the visual re-gate. It is
   a **data** change (reflect every authored box) independent of the movement *logic*
   below, so it lands cleanly early. *Checkpoint:* boxes reflected; geometry goldens
   re-baselined; combat/advantage goldens **unchanged**; harness/sim shows the pushbox at
   the body's lower part and the crouch box shrinking downward.
3. **TKT-P1.1R-03 (held-input stances)** — the **AD-038** engine change (phase-2 loop-state
   exit) plus the crouch-stance wiring; walk now stops on release and crouch enters/exits.
   Carries the only engine edit, so it is isolated. *Checkpoint:* harness asserts walk-and-
   stop both ways and crouch enter-on-hold / stand-on-release.
4. **TKT-P1.1R-04 (airborne actions)** — the **AD-039** content wiring: forward/back &
   diagonal jumps and air-normal reachability. Pure `content/character_a.gd`. Last because
   it is the most content and benefits from the harness + a settled state machine.
   *Checkpoint:* harness asserts `JUMP_F/JUMP_B` reachable on `9/7`, and `j.L/M/H`
   reachable in the air.

**TKT-P1.1R-05 (added post-hoc — AD-038 exit-lag correction)** follows the four above. It came
out of the end-of-feature ratification pass (the AD-038 held-stance exit was ruled an oversight and
AD-038 corrected), so it was ticketed after -01..-04 rather than in the original decomposition. It
refines the -03 loop-state branch (depends on -03), is **independent of -04**, and must land
**before** the QA audit and the human re-gate. *Checkpoint:* harness asserts a held walk/crouch
exits to idle on the release frame (not ~5 ticks later), with AD-022 discrete-command leniency
intact.

**The dash is NOT a ticket.** The brief's *discretionary* forward/back dash is a
Strategist/user **scope** call, raised as a flag (`flags.md`, owner: Strategist) — the
dash **states** exist and are correct data, but the input to reach them (double-tap `66`/
`44`) is unbuilt and would need a **new** recognizer mechanism. Do not build it under this
file until the scope flag resolves.

**After all four:** QA runs the objective audit — the movement/geometry changes **move sim
behavior and box geometry deliberately** (goldens change, JC-017 style; the *relationships*
the combat goldens test are unchanged by the uniform box reflection). Then the **user
re-gate** drives the brief checklist (`character-a-movement-reconciliation.md` →
"Acceptance"). QA cannot mark P1.1 done while the gate is open; only the user closes it.

**Deferred / parked (confirm at the re-gate, do not lose):** the two feel flags
(frame-step auto-pause JC-045; jump apex-hang JC-047) and the crouching-normal-height flag
(likely closes once AD-037 lands — re-evaluate against a correctly-oriented display, don't
close it before). All non-blocking; all Strategist's to close.

---

### TKT-P1.1R-01 · Scripted-input behavioral-trace harness (minimal)
**Serves:** `/docs/spec/trace-harness.md` (all three contracts + acceptance 1–6); Tenet 2;
AD-011 (reads only through `InspectionView`); AD-019 (float-free trace). **Depends:**
nothing new — `RecordPlaybackSource` (JC-030), `InspectionView`, `MoveRegistry`, the
`TrainingHarness`/`TickHost` produce-before-query wiring all exist.
**Scope (minimal — hold the line):**

- **`InputScript` compiler** (`compile(text) -> PackedInt32Array`, a pure `class_name` of
  static helpers, sibling to `InputFrame`): the numpad+`L/M/H`+`*count`+`#`-comment grammar
  in the spec, RAW/screen-relative numpad, per-tick motion authoring, hard-error on a
  malformed token or reserved bit (validated at the boundary, `input.md` criterion 6).
- **Headless driver**: compile P1 (and optional P2, default neutral) → buffer(s); load each
  into a `RecordPlaybackSource` via `set_recorded_buffer` + `Mode.PLAYBACK`; install the
  roster; build the initial `SimState` with both players as the character under test in idle
  (same wiring TKT-P1.1-01 fixed); run N ticks with `produce_next()`-then-`step`.
- **Trace dump + inline-assert runner**: build a trace row per tick from `InspectionView`
  (the default movement field-set; optional `boxes`/`advantage`/`last_hit`), fixed field
  order, **no floats**; support inline `(tick, field, expected)` assertions that fail loudly
  with tick/field/expected/actual.
- **Not now:** P2 mirroring, `.trace` golden tooling, dummy AI, any non-`RecordPlaybackSource`
  replay path.

**Acceptance:** `trace-harness.md` criteria 1–6. Include a smoke script (idle stays idle;
`6*n` moves `pos_x` forward) proving compile→replay→assert end-to-end and source-equivalent /
deterministic replay (criterion 3). **Judgment-log:** record any latitude (grammar edge
cases, the trace row exact text) for ratification.

### TKT-P1.1R-02 · Geometry Y-fix — box-data reflection (AD-037)
**Serves:** **AD-037**; `move-format.md` → `Box` (vertical convention); `training-mode.md`
criteria 5/14 (boxes render right-side-up). **Depends:** TKT-P1.1R-01 (to assert sim box
orientation through the harness; render itself is the human gate). Independent of the
movement-logic tickets.
**Scope:**

- **Reflect every authored box across the feet line** (`new_y = −(old_y + old_h)`, `h`
  unchanged) in `content/character_a.gd` — hurtboxes (`_hurt_stand`/`_hurt_crouch`/
  `_hurt_air`), the default pushbox, every normal/special/throw hitbox, and every spawn
  offset that is a vertical body reference. **Do the same for the P0 test character**
  (`tests/test_support.gd`) so the one box convention holds slice-wide (AD-037).
- **No engine/format code change.** `resolve_box`, `ResolvedBox.overlaps`, and `px_rect`
  already assume the shared-axis translate (up = −Y); do **not** flip any render sign
  (AD-037's load-bearing "rejected": a render flip double-inverts the jump).
- **Re-baseline geometry goldens** (box world-y changes deliberately, JC-017 style). The
  combat/advantage/determinism goldens must stay **unchanged** — the reflection is uniform,
  so hit/hurt *relationships* (and thus every combat outcome) are identical; if a combat
  golden moves, something is wrong (a non-uniform reflection), stop and raise it.

**Acceptance:** AD-037; `training-mode.md` criteria 5/14. Headless: a harness/test asserts
the sim-truth orientation now reads right-side-up (e.g. the crouch hurtbox's top edge is
*above* — more negative y — nothing, but its extent shrinks toward the feet; the pushbox
occupies the body's lower — nearer-to-feet — portion of the hurtbox). **Pixel visibility /
upright rendering is confirmed at the human-inspection re-gate** — not headless-checkable
(the whole point). **Judgment-log:** record any box whose "correct" reflected height is a
judgment (e.g. a spawn offset) for ratification.

### TKT-P1.1R-03 · Held-input stances — walk exit + crouch stance/block (AD-038)
**Serves:** **AD-038**; AD-032 (pure-direction command — the entry half); `combat-resolution.md`
phase 2; `character-a.md` → Movement ("standing and crouching blocking"). **Depends:**
TKT-P1.1R-01 (verify through it). Carries the only engine change in this file.
**Scope:**

- **Engine (AD-038):** in `step_phases.gd` `phase2_state_machine`, the actionable branch —
  when `Actionability.is_actionable(p, move)` **and** `move.loop`, re-derive the desired
  state each tick: `target = _buffered_command(...)`; if `target == -1`, `target =
  character.idle_state_id`; transition iff `target != p.state_id`. Non-loop actionable states
  keep the existing behavior. This gives walk **and** crouch their release→idle exit. Keep it
  character-agnostic (reads `move.loop` + `idle_state_id`, no character-A branch).
- **Content:** add the bare-`DOWN` pure-direction `button_map` entry `_map(-1, DOWN, 0,
  STATE_CROUCH)` so held `2` enters the existing `CROUCH` state. **Ordering** (AD-032
  first-match-wins): after the `DOWN+button` crouch normals (so `2L/2M/2H` still win when a
  button is held) and **before** the walk entries (so a down-forward `3` crouches, not walks).
- **Crouch block falls out** (no new mechanism): `_defender_is_blocking` already blocks on a
  held-back input in any non-hitstun actionable state, so once the crouch **stance** is
  reachable, a crouching defender holding down-back blocks (stand block already works). Note:
  blocking is **stance-agnostic hold-back** in the slice (no high/low enforcement) — that is
  the current model, not a gap this ticket introduces; a high/low block system, if ever
  wanted, is a Strategist scope call (do **not** add it here).

**Acceptance:** AD-038; `character-a.md` Movement. Harness assertions (brief checklist):
walk forward enters on held `6` and returns to idle on release; walk back likewise on `4`;
crouch enters on held `2` (crouch hurtbox resolves) and returns to stand on release; a
crouching held-back defender blocks a hit (enters a blockstun category). Determinism
unchanged (pure function of history + state); movement goldens change deliberately (walk now
terminates). **Judgment-log:** record any latitude (crouch entry ordering, block-state edge
cases) for ratification.

### TKT-P1.1R-04 · Airborne actions — directional/diagonal jumps + air normals (AD-039)
**Serves:** **AD-039**; AD-032 (composite pure-direction command); AD-015 (`CancelRule`);
`character-a.md` → Movement ("jumping neutral/forward/back … jump-in normals"). **Depends:**
TKT-P1.1R-01 (verify), and cleanest after TKT-P1.1R-03 (shares `content/character_a.gd`
`button_map`). Pure content — no engine/format change.
**Scope:**

- **Directional/diagonal jumps:** author `PREJUMP_F`/`PREJUMP_B` mirroring the existing
  `PREJUMP` (each an `input = 0` ALWAYS cancel, window `[3,3]` per JC-038, into `JUMP_F`/
  `JUMP_B` respectively). Add composite-direction `button_map` entries: `UP|FORWARD →
  PREJUMP_F`, `UP|BACK → PREJUMP_B`, and keep `UP → PREJUMP_N` — the **two diagonals listed
  before** bare `UP` (first-match-wins). `9` (up+forward) = forward jump; `7` (up+back) = back
  jump — the same motion the brief calls "forward/back" and the checklist calls "diagonal."
- **Air normals:** add three `CancelRule`s to each of `JUMP_N/F/B` — condition `ALWAYS`,
  `window` = the airborne frames (e.g. `[1, JUMP_DURATION − 1]`), `input =` `BUTTON_0/1/2`
  bitmask → `STATE_JL/STATE_JM/STATE_JH`. No `button_map` entry for the air normals is needed
  (`CancelEval`'s raw-button fallback resolves `rule.input == BUTTON_n`).
- **Known limitation (AD-036 class, do NOT build around it):** an air normal ends → idle at
  whatever height the arc/normal left it (no ground-contact landing — AD-036 deferred).
  Reachability + rendering is the bar; if the post-air-normal float reads badly at the gate it
  is an AD-036 roadmap question (Strategist), not this ticket.

**Acceptance:** AD-039; `character-a.md` Movement. Harness assertions: `9`→`PREJUMP_F`→
`JUMP_F` (pos_x carries forward), `7`→`PREJUMP_B`→`JUMP_B` (carries back), `8`→`JUMP_N`
(neutral, arc nets zero — JC-047); a button pressed mid-jump enters the matching `j.*`
(category AIRBORNE, reachable). Neutral-jump arc still lands flush (pos_y returns to
`ground_y`). **Judgment-log:** record any latitude (air-normal cancel window bounds, prejump
authoring) for ratification.

### TKT-P1.1R-05 · Held-stance exit reads current-tick input (AD-038 correction)
**Serves:** **AD-038** → "Correction (2026-07-10 …)" and its updated "Consequence (Developer)"
(the loop-state re-derivation reads current-tick input, not the command buffer); AD-022 (the
command buffer is reversal/cancel *entry* leniency, not stance selection); resolves the archived
AD-038 exit-lag flag. **Depends:** TKT-P1.1R-03 (the AD-038 loop-state branch this refines
already exists there). **Independent of TKT-P1.1R-04** (airborne is already built and untouched
by this). Carries an engine change (same `phase2_state_machine` loop-state branch as -03).
**Scope:**

- **Engine (AD-038 corrected contract):** in `step_phases.gd` `phase2_state_machine`, make the
  actionable-**and**-`move.loop` branch two-tier:
  1. If a **discrete** command (a recognized command whose target state is **not** `loop`) is
     buffered-ready via the AD-022 command buffer, transition to it — **unchanged** AD-022
     leniency, and it takes priority when one is buffered-ready.
  2. Otherwise derive the stance `target` from the **current tick's** recognized pure-direction/
     `loop` command — **NOT** the buffered command window — and if none is satisfied *this tick*,
     `target = character.idle_state_id`; transition iff `target != p.state_id`.
  The `loop` / non-`loop` target split is the discriminator (`MoveRegistry` already carries each
  state's `loop` flag). Keep it **character-agnostic** (reads `move.loop` + `idle_state_id` +
  the target's `loop` flag; no character-A branch). Non-loop actionable states keep their
  existing "run a buffered command, else stay" behavior.
- **Net behavior:** a released walk/crouch direction returns to idle on the **very next actionable
  tick** (prompt release, no command-buffer carry-over) instead of lingering ~5 ticks; a held
  direction still re-selects its stance every tick (`target == current`, no-op); a slightly-early
  *discrete* press still fires on the first actionable frame (buffer leniency intact).
- **Re-baseline the walk/crouch release-timing goldens** (they move **deliberately** — this is an
  objective behavior change, walk/crouch now stop on the release frame, not ~5 ticks later; JC-017
  style). Combat/advantage/determinism goldens and the *held*-direction movement goldens (walk
  integrates while held) must stay **unchanged** — only the release-frame timing moves; if a
  combat or held-walk golden shifts, something is wrong, stop and raise it.

**Acceptance:** AD-038 (the corrected contract). Harness assertion expressing the fix — walk held
N ticks then released exits to idle **on the release frame's next actionable tick**, not
`COMMAND_BUFFER − 1` ticks later, e.g.:

```
# walk forward 5 ticks then release → idle immediately, not ~5 ticks later
P1: 6*5 5*6
assert tick=5  p0.state=WALK_F
assert tick=6  p0.state=IDLE        # released at 6; idle THIS actionable tick, not tick 11
```

Same for crouch (`2*5 5*6` → `CROUCH` then `IDLE` on release). And a **regression guard** that
discrete-command leniency survives: a special/normal buffered slightly before an actionable frame
still fires (the existing reversal/cancel-buffer tests must stay green — do not weaken AD-022).
Determinism unchanged (still a pure function of `input_history` + state). **Judgment-log:** record
any latitude (how "buffered-ready discrete command" is detected; the exact current-tick recognizer
call) for ratification.

**Sequence:** lands **before** the QA objective audit and the human re-gate (P1.1's re-gate should
not run on the ~5-tick walk-stop lag). Dispatched after ratification, independent of -04.

---

## Cross-cutting (verified at feature audit / human re-gate, not separate tickets)

- **Human re-gate (roadmap P1.1, `audit-criterion.md`):** after all four land, the user runs
  `training_mode.tscn` and **drives the brief checklist** — A walks and stops both ways,
  crouches (stance + block), jumps in all directions and lands flush, its normals are
  reachable, and **all boxes render right-side-up**. QA cannot issue "done" while this stands;
  only the user closes it. The trace harness (TKT-…-01) makes the *sim-behavior* half of the
  checklist executable; the *render* half is the human's.
- **Deferred feel calls** (frame-step auto-pause JC-045; jump apex-hang JC-047) and the
  **crouching-normal-height** flag are **confirmed at the re-gate**, not before (the height
  flag likely vanishes once AD-037 orients the display — re-evaluate then).
- **Judgment-call log:** any latitude on these tickets is recorded for Architect ratification
  before the audit (protocol cadence).

---

# Re-gate 3 batch (2026-07-11) — second reconciliation batch (BATCHED dispatch)

> Owned by the **Architect**. Decomposed from the reconciliation work-order's **"Re-gate 3
> findings (2026-07-11)"** section (D1/D2/D3/Q1/Q2) after diagnosing each against spec + code.
> Batch 1 (`TKT-P1.1R-01..05`) is delivered, QA-passed, pushed; re-gate 3 confirmed most of the
> checklist good and surfaced this remaining set. **New ruling this pass:** **AD-040**
> (dummy-control operability model). The human-inspection gate stays **open**; P1.1 is **not**
> done on headless green.

## Diagnosis outcome (what became a ticket, and why)

| Finding | Classification | Disposition |
|---|---|---|
| **D1** dummy uncontrollable → crouch-block unverifiable | **P1.1-scope spec gap + wiring** (AD-040) | **TKT-P1.1R2-01** |
| **D2** clean jumps "sometimes" land off floor | **REFUTED** — arc-data bug does not exist | **No data fix.** Optional regression guard **TKT-P1.1R2-02**; refutation flagged to Strategist |
| **D3** aerials freeze + float | **Confirmed AD-036 gap** | **Deferred to P2** (not ticketed) — I concur with the Strategist's classification |
| **Q1** DP doesn't rise | **Design-intent + roadmap call** (spec ambiguous; character identity) | **Raised to Strategist** (flag) — not a unilateral P1.1 data fix |
| **Q2** H DP two hitboxes | **Intended** (spec authors `623H` "Active 10 (2 hits)") | **Confirmed intended, no action** |

**D2 refutation (empirical).** All three jump arcs (`JUMP_N/F/B`) share **one identical net-zero
`vel_y` profile** — `_build_jump_arcs()` applies the same rise/hang/fall to every state; only
`motion_vel_x` differs by direction. Driven headless, a **clean** jump in every direction lands
flush at `py = 0` (neutral/forward/back all return to idle at ground_y). The Strategist's
hypothesis (F/B arcs never got JC-047's net-zero fix) is **wrong** — F/B inherited the fix by
construction. The "sometimes off floor" the user saw is the **AD-036 aerial-interruption float
(= D3)**: an air normal ends → idle at the arc's current height (confirmed headless: an
air-normal-interrupted jump returns to idle at `py ≠ 0`, actionable, floating). So D2 as reported
does not reproduce; its off-floor cases are D3, held to P2.

## Sequencing (BATCHED WHOLE — Strategist steerability override for the budget experiment)

**This batch is dispatched as ONE Developer session, both tickets in order, ending on ONE
checkpoint** — deliberately **not** the P1.1/P2 per-ticket default (protocol § "Token economy").
Rationale: the user is running a **batched-dispatch budget experiment** (re-examining the
per-ticket default), and this pair is a natural fit — high spec-read overlap (both live in the
training-mode / character-A operability surface the Developer will already have loaded), tiny
combined blast radius (one source-wiring change + one headless-assertion addition, **no engine or
format change**), and both land on the same verifiable checkpoint. Per the Architect's dispatch
ownership, the Developer executes this order and does **not** re-split it.

```
01 dummy-control wiring (D1, AD-040)  ─▶  02 jump net-zero regression guard (D2)
   (inject dummy live sampler;             (assert all-direction flush landing;
    record→playback puppeting)              no data change — hardening)
```

- **-01 first:** it is the real operability fix and touches `training_mode.gd` +
  `controls_legend.gd` + `project.godot` (if a dummy sampler action is added). **-02 second:** a
  pure headless-test addition in `tests/test_airborne_actions.gd`, independent of -01, cheap, ends
  the session green. Order is for read-locality, not a hard dependency.
- **Checkpoint (whole batch):** `run_tests.bat` green (existing 31 + the new -02 assertions);
  the dummy is human-drivable in-scene (record→playback makes it hold crouch-block) — the latter
  confirmed at the **human re-gate**, since dummy operability is not headless-checkable (same class
  as criterion 13). QA runs the objective audit after; the human re-gate stays open.

**-02 is OPTIONAL (Strategist's call).** D2 is refuted, so strictly no fix is owed. -02 adds no
behavior — it closes the *verification* hole the Strategist named ("a net-zero assertion for EVERY
jump direction"): today only `JUMP_N`'s flush landing is asserted, so a future edit to the F/B arcs
would land off-floor un-caught. Near-zero cost, honest hardening. Drop it from the batch if the
budget experiment wants the minimal cut; nothing else depends on it.

---

### TKT-P1.1R2-01 · Dummy-control wiring — make the dummy human-drivable (AD-040)
**Serves:** **AD-040**; `training-mode.md` → "Human control surface" (Driving the dummy) +
criterion 13; the re-gate-3 D1 finding. **Depends:** nothing new — `RecordPlaybackSource`'s
injected-`Callable` live-sampler path and the `M` mode-cycle binding both already exist; this
supplies the one missing piece (the dummy's live sampler). **No engine/format change.**
**Scope:**

- **Inject a live device sampler into the dummy source.** In `TrainingMode._ready()`, the dummy
  (`_source_p2`) is constructed as `RecordPlaybackSource.new()` with **no** sampler, so
  `RECORDING`/`PASSTHROUGH` emit `NEUTRAL` and cycling `M` is inert-in-effect. Give the dummy a
  live device sampler (reuse `_sample_device_p1`'s shape, or a dedicated `_sample_device_dummy`
  if the Developer wants the dummy on a distinct key set — Developer latitude, placeholder keys
  like all others). After this, cycling the dummy to `RECORDING` captures the human's device
  frames; `PLAYBACK` loops them. Route everything through the shell's existing
  `set_dummy_mode`/`_cycle_dummy_mode` (do **not** touch `RecordPlaybackSource` from outside the
  shell — the seam rule, criterion 10-adjacent).
- **Keep exactly one `InputSource` (Tenet 2).** No dummy AI, no second-human-takeover model, no
  P2-special type — the dummy stays a `RecordPlaybackSource` fed by an injected `Callable`, the
  class's documented pattern. If any part of this strains Tenet 2, **raise it** (do not work
  around it).
- **Discoverability (minimal).** Update `ControlsLegend` so the record→playback *workflow* is
  legible, not just the mode name — one added hint line is enough (e.g. "Record dummy: cycle to
  Recording, input a sequence, cycle to Playback to loop"). Legend still reads key text from
  `InputMap` (no drift), still carries no seam dependency.
- **If a new input-map action is added** (dummy sampler on its own keys), add it to
  `project.godot` and to the legend's action list; keep key choice placeholder.

**Acceptance:** AD-040; `training-mode.md` criterion 13 (dummy human-drivable). Headless where
possible: a test/harness asserts that a dummy source **with an injected sampler** in `RECORDING`
captures non-neutral frames and `PLAYBACK` loops them (the `RecordPlaybackSource` round-trip already
has coverage — extend it to the *shell-wired* dummy so the regression is at the operability layer,
not just the class). **Operability itself — a human cycling `M` to record a crouch-block and the
dummy then holding it — is confirmed at the human re-gate** (not headless-checkable; the D1 defect
was invisible to headless for exactly this reason). **Judgment-log:** record any latitude (dummy
key set; whether P1 also mirrors the device during dummy `RECORDING`, and if not, how P1 is held
neutral) for ratification.

### TKT-P1.1R2-02 · Jump net-zero landing — regression guard for EVERY direction (D2)
**Serves:** the D2 refutation (close the verification hole); `character-a.md` → Movement (jump arcs
net zero, JC-047); AD-036 (the interim net-zero-arc **authoring invariant** — this guards it for A
in every direction). **Depends:** the trace harness (`TKT-P1.1R-01`, delivered) and the airborne
actions (`TKT-P1.1R-04`, delivered). Independent of `-01`. **No data/engine change — test only.**
**Scope:**

- **Assert flush landing for all three jump directions.** `test_airborne_actions.gd` currently
  asserts net-zero flush landing only for the **neutral** jump (`_test_neutral_jump…lands_flush`).
  Add the same `p0.py` **returns to `ground_y` (0)** assertion after the arc completes for the
  **forward** (`9`) and **back** (`7`) jumps (the existing F/B tests check only horizontal carry).
  A held-then-released `9*3 5*<to landing>` / `7*3 5*<…>` script, asserting `p0.py == 0` once the
  jump returns to `STATE_IDLE` — mirroring the neutral test's shape.
- **Optionally** fold the three into one table-driven check so a future fourth direction is one
  row. Keep it a plain assertion (no golden file).

**Acceptance:** all three clean jump directions land flush (`py` returns to `ground_y`) via harness
assertions; the suite fails loudly if any direction's arc stops netting zero (the guard the
Strategist asked for). This is **hardening, not a bug fix** — clean jumps already land flush
(verified this pass); the ticket ensures they *keep* doing so per direction. **Judgment-log:** none
expected (pure assertion addition); record if the landing-detection tick differs by direction.

---

## Re-gate 3 — deferred / raised (not tickets; Strategist's to resolve)

- **D3 (aerial float) — held to P2.** Confirmed the **AD-036 gap** (no ground-contact landing; an
  air normal ends → idle at the arc's height). I **concur** with the Strategist's classification:
  pulling a bare clamp into P1.1 would mask a mis-authored arc (the anti-legibility AD-036 explicitly
  rejects), and the clamp+landing-semantics unit is P2's opening work. **Not ticketed.** It does not
  block P1.1's *operability* — the character is fully controllable; the float is a stated, roadmapped
  limitation (AD-036 / AD-039 "Known limitation"), not a broken instrument. No flag needed beyond
  this confirmation.
- **Q1 (DP doesn't rise) — RAISED to Strategist (character identity + roadmap).** The DP is authored
  **grounded** (no `motion_vel_y`), but the spec **prose** gestures at airborne-ness — the recovery
  column is "`28 + 12(land)`", the text says "full **landing** recovery," and the active keyframe
  comment says "DP leaves the ground." Yet no rise trajectory is authored and no AD covers DP
  verticality. Critically, a **rising** DP would need the exact airborne→ground **landing mechanism
  AD-036 defers to P2** — it would rise-and-**float**, reproducing D3. So the architecturally coherent
  reading is: the DP is grounded **for the slice** *because* the landing mechanism doesn't exist yet,
  and the "+land" recovery models the landing-recovery **duration** as data without actual airborne
  motion (parallel to how the jump arc and air normals handle "landing" as data). **This is a
  character-identity call (brief-owned) with a roadmap consequence, so I do not default it.**
  **Recommendation to the Strategist:** rule the grounded DP **intended for P1.1** (→ intended,
  no action here; I then reconcile the spec prose so "+land"/"leaves the ground" reads as authored
  recovery, not an un-built rise). If instead A's signature reversal **should** rise, it **binds to
  AD-036** and defers to **P2** alongside D3 — **not** a P1.1 data fix (adding `vel_y` now yields a
  floating DP). Either way, not ticketed this batch.
- **Q2 (H DP two hitboxes) — intended, no action.** `character-a.md` authors `623H` as "Active 10
  (**2 hits**)" / "2-hit launch," and the build matches (two id_groups: `IDG_DP_H_HIT1`,
  `IDG_DP_H_HIT2`, each landing once per AD-016). The user's "fine if intended" is satisfied.

---

# Re-gate 4 batch (2026-07-11) — third reconciliation batch (BATCHED dispatch)

> Owned by the **Architect**. From the work-order's **"Re-gate 4 findings"** section (E1/E2 +
> frame-step auto-pause). The R2 batch passed QA **headless** but two items **still failed live** at
> the 4th human re-gate — the recurring "green headless, broken in the hands" gap. New rulings:
> **AD-041** (dummy-mode observability + fresh-record), **AD-042** (minimal ground-contact landing).
> Human-inspection gate stays **open**; P1.1 is **not** done on headless green — and E1/E2 are
> **inherently live-gated** (a rendered indicator and pixel-flush landing are not headless-confirmable).

## Diagnosis outcome

| Finding | Verdict | Disposition |
|---|---|---|
| **E1** dummy uncontrollable live | Live record *wiring is correct*; root cause is **no on-screen mode indicator** (charter observability) + a **latent re-record concatenation** bug (AD-041) | **TKT-P1.1R3-01** |
| **E2** jumps wedge off-floor | **GENUINE P1.1 bug** (not the aerial case): held/repeated jumps drop each arc's final fall frame → **+6 units up per jump**, confirmed via harness | **TKT-P1.1R3-02** (AD-042 ground-snap); **scope fork flagged** (pulls AD-036 landing half forward, resolves D3) |
| **Frame-step auto-pause** | Decided (user wants it) | **TKT-P1.1R3-03** (binding change) |

**E1 diagnosis (evidence).** `training_mode.gd._physics_process` produces **both** sources every
running tick (P1 works live off the adjacent line, proving the loop runs); in `RECORDING` the dummy
samples `_sample_device_dummy` and appends; `PLAYBACK` feeds it. The dummy keys (WASD+U/I/O) and the
record→playback workflow are already in `ControlsLegend`. **So the wiring is not the bug.** The bug
is that a human cycles `M` through three modes **with zero live feedback** — can't tell if RECORDING
is armed — so "only control P1 in PLAYBACK" (a neutral buffer) is indistinguishable from broken. Plus
`RECORDING` **appends** to the stale buffer on every re-take (no clear on entry, cursor not reset), so
retries concatenate = "inconsistent." → visible mode indicator + fresh-record-on-entry (AD-041).

**E2 diagnosis (repro).** Trace-harness battery across scenarios the isolated R2 test omits.
`8*100` (**hold UP** — the natural repeated-jump input): JUMP_N begins at `py=-6`, re-enters PREJUMP
at `-6`, next jump `-12`, then `-18` — **drifts +6 up per jump, never recovers.** A single jump with
a release gap (`8*3 5*80`) and a re-pressed jump with a gap (`8*3 5*46 8*3 5*46`) both land flush
(`py=0`). **Root cause:** a **held** jump transitions from its `duration` frame straight back into a
grounded state (idle-rederive → prejump, AD-038) with no settled idle tick, dropping the arc's final
fall frame (`+6`). The arc is correctly authored (net-zero over a full play); the **transition drops
a frame**. The isolated R2 assertion releases UP after 3 ticks, so it never reproduces. → minimal
ground-contact landing snap (AD-042).

## Sequencing (BATCHED WHOLE — user's steer)

**One Developer session, all three tickets in order, one checkpoint** — not per-ticket. High
spec-read overlap (all three live in the training-mode shell + character state machine the Developer
will already have loaded); the Architect owns dispatch and the Developer does not re-split.

```
01 dummy observability (E1, AD-041)  ─▶  02 ground-contact landing (E2, AD-042)  ─▶  03 frame-step auto-pause
   (mode indicator + fresh-record;         (grounded-entry py→ground_y snap;           (N: pause then step —
    live-gated)                             headless-verifiable + live-gated)            binding one-liner)
```

- **-01 first:** the highest-risk-of-recurrence item and the one most needing the live gate; get it
  in early so the 5th re-gate has the most time to confirm. **-02 next:** the engine snap; ends
  headless-green (harness proves held/repeated jumps land flush). **-03 last:** a tiny binding change.
- **Checkpoint (whole batch):** `run_tests.bat` green (existing 31 + new E2/landing assertions);
  **live re-gate confirms** the mode indicator, the record→playback round-trip in-app, and flush
  landing on held/repeated jumps. Mark clearly (below) what is headless vs. live-only.

**Headless vs. live split (state it — E1 is why we're here twice):**
- **Headless-verifiable:** E2's ground-snap (harness asserts held/repeated/interrupted jumps land at
  `ground_y`); fresh-record buffer semantics (a shell-level test: two record passes → the second
  replaces, not concatenates); frame-step auto-pause (a paused-state assertion after the bound call).
- **Live-only (5th re-gate):** the **rendered mode indicator** (pixels), the **in-app** record→
  playback round-trip through the real `_physics_process`/GUI path, and **pixel-flush** landing. These
  cannot be headless-closed — the fix is built to be as **observable** as possible so the human *can*
  confirm.

---

### TKT-P1.1R3-01 · Dummy-mode indicator + fresh-record (E1, AD-041)
**Serves:** **AD-041**; `training-mode.md` → "Human control surface" (Dummy-mode indicator +
fresh-record); charter clarity (mode state observable); re-gate-4 E1. **Depends:** the AD-040 dummy
wiring (delivered). **No sim/format change.**
**Scope:**

- **Visible live mode indicator.** Add a minimal on-screen readout of the dummy's current mode
  (PASSTHROUGH / RECORDING / PLAYBACK) with a distinct **recording tell** (e.g. "● REC"), updated
  live as `M` cycles. Read the mode through the shell (`get_dummy_mode`) — **outside** the
  `InspectionView` seam (mode is not sim state; same placement as `ControlsLegend`, criterion 10
  N/A). Keep it minimal (one label), legibility of the instrument, not polish.
- **Fresh-record on RECORDING entry.** On the transition *into* `RECORDING` (PASSTHROUGH/PLAYBACK →
  RECORDING), the shell **clears the dummy buffer and resets its playback cursor** so a re-take
  replaces the prior recording (not concatenate). Orchestrated in the **shell** (`_cycle_dummy_mode`
  / `set_dummy_mode`), keeping `RecordPlaybackSource`'s `RECORDING`-appends primitive unchanged
  (use the existing `set_recorded_buffer([])` + cursor reset, or a small "begin_fresh_recording"
  shell step). Tenet 2 intact.
- **Do not** move the indicator behind `InspectionView`, and **do not** clear the buffer mid-RECORDING
  (only on entry).

**Acceptance:** AD-041; `training-mode.md`. **Headless:** a shell-level test asserts two record
passes → the second buffer **replaces** the first (fresh-record), and that entering RECORDING resets
the cursor. **Live (5th re-gate):** the mode indicator is visible and tracks `M`; a human records a
crouch-block and PLAYBACK loops it (the record→playback round-trip works in-app). **Judgment-log:**
record any latitude (indicator placement/labels; exactly where the fresh-record clear is invoked).

### TKT-P1.1R3-02 · Minimal ground-contact landing snap (E2, AD-042)
**Serves:** **AD-042**; AD-036 (its landing half, pulled forward); `character-a.md` → Movement (clean
jumps land flush); re-gate-4 E2. **Depends:** TKT-P1.1R2-02 (the per-direction net-zero assertion —
authoring guard that keeps this from masking arc bugs). **Carries an engine change** (state machine).
**Scope:**

- **Grounded-entry snap.** In `step_phases.gd` phase 2, on **entry to a `GROUNDED`-category state**
  (a state transition where the target's `MoveState.category == CATEGORY_GROUNDED`), if
  `pos_y != ground_y`, set `pos_y = ground_y`. Character-agnostic (reads the target's category and
  `StageState.ground_y`; no character-A branch). Deterministic (pure function of state).
- **This is the landing *snap* only** — **not** the full per-tick `pos_y ≥ ground_y` clamp and
  **not** variable-height air-move semantics (both stay deferred to P2, AD-036). Do not add them.
- **Side effect (intended, per AD-042):** an air normal ending mid-arc now also snaps to floor — this
  **resolves re-gate-3 D3** (aerial float). That is expected; do not treat the D3 change as a
  regression.

**Acceptance:** AD-042. **Headless (widen the harness — this is the core ask):** assert flush landing
(`py == ground_y`) across **held UP** (`8*N`), **held forward/back** (`9*N`/`7*N`), **repeated
jumps**, and an **air-normal-interrupted** jump — the scenarios the isolated R2 test omits and the
human hits. Held/repeated jumps must not drift. Determinism unchanged; held-jump position goldens
move **deliberately** (JC-017 style) — if a *non-jump* combat/advantage golden moves, stop and raise
it. **Judgment-log:** record the exact grounded-entry hook point and any category-edge latitude.

### TKT-P1.1R3-03 · Frame-step auto-pause (decided)
**Serves:** `training-mode.md` → "Frame control" (frame-step auto-pause); supersedes the provisional
JC-045 non-auto-pause binding; re-gate-4 (user wants it). **Depends:** nothing. **Binding-only
change** (the `step_once()` method is unchanged).
**Scope:**

- The bound frame-step control (`N`, `tm_step`) calls `set_paused(true)` **then** `step_once()`, so a
  human steps from a running sim in one press. Keep it in the **binding** (`_unhandled_input`), not
  the `step_once()` method (which other paths call). Update the archived JC-045 feel flag as resolved
  (owner relays); the controls legend text needs no change (still "Frame-step").

**Acceptance:** `training-mode.md`. **Headless:** after invoking the bound step handler from a running
state, `is_paused()` is true and exactly one tick advanced. **Live:** confirmed incidentally at the
re-gate. **Judgment-log:** none expected.

---

## Re-gate 4 — raised to Strategist (scope fork; not decided here)

- **AD-036 pull-forward (E2/AD-042).** The minimal ground-contact landing snap fixing E2 **is the
  landing-semantics half of AD-036**, which re-gate 3 deferred to P2 — and as a side effect it
  **resolves D3** (the aerial float, also deferred). Pulling it into P1.1 is a **roadmap/scope call**
  (owner: Strategist), raised in `flags.md`. **My recommendation: pull it in** — a natural input
  (hold-to-jump) wedges the character off-floor, so the P1.1 gate genuinely needs it; the snap is
  legible (net-zero authoring invariant + TKT-P1.1R2-02's per-direction assertion still catch
  mis-authored arcs, so it doesn't mask them); and the *full* runtime clamp + variable-height air-move
  semantics stay P2's. If the Strategist would rather re-defer E2 or scope it narrower, that redirects
  TKT-P1.1R3-02. The AD is marked settled *pending this confirmation*.
