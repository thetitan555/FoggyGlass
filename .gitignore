# Consultant workspace: derived copies of the foundational docs, regenerated by
# sync-consultant-context.bat. Single source of truth is /docs/*.md — these
# copies are local convenience only, never committed, so they can't drift.
docs/roles/consultant-corner/charter.md
docs/roles/consultant-corner/principles.md
docs/roles/consultant-corner/technical-tenets.md

# Checkpoint request: a role writes its proposed commit message here; commit.bat
# / push.bat read it and delete it. Transient, never tracked.
COMMIT_MSG.txt
